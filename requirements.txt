# Geospatial core (GDAL comes from the base image)
rasterio>=1.3
rioxarray>=0.15
xarray>=2024.1
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
fiona>=1.9
numpy>=1.26,<2       # keep <2 for ABI compat with the base image's GDAL bindings
scipy>=1.11
scikit-image>=0.22

# Terrain analysis
whitebox>=2.3          # slope/aspect/TPI/TWI/cost-distance/least-cost paths

# Remote sensing / STAC
pystac-client>=0.7
planetary-computer>=1.0
mercantile>=1.2

# Data acquisition
requests>=2.31
osmnx>=1.9             # OSM roads/tracks

# Config & CLI
pydantic>=2.6
pyyaml>=6.0
typer>=0.12
rich>=13.7

# Export
gpxpy>=1.6            # GPX for OnX
simplekml>=1.3       # KML polygons/lines for OnX web + web map
jinja2>=3.1          # GeoPDF/brief templating
pypdf>=4.0           # parse MFFP état-de-situation PDFs (effort/success/density)
matplotlib>=3.8      # rendered field map

# Dev
pytest>=8.0

# API
fastapi>=0.110
uvicorn>=0.29
